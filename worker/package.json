{
  "name": "gravityflow-worker",
  "version": "2.0.0",
  "description": "GravityFlow BullMQ worker service (Render-compatible)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node index.ts",
    "build": "tsc --outDir dist",
    "start": "node dist/index.js",
    "health": "curl -s http://localhost:${PORT:-8080}/health | node -e \"process.stdin.resume(); process.stdin.on('data', d => { const j = JSON.parse(d); console.log(j.status === 'ok' ? '✅ Healthy' : '❌ Unhealthy'); process.exit(j.status === 'ok' ? 0 : 1); })\""
  },
  "type": "commonjs",
  "dependencies": {
    "bullmq": "^5.0.0",
    "dotenv": "^17.4.2",
    "ioredis": "^5.3.2",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
